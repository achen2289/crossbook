# Polymarket US API credentials (generate at https://polymarket.us/developer).
# Optional: without them Polyscope runs in public-data mode and the Account
# panel is disabled. Never commit your real .env.
PMUS_KEY_ID=
PMUS_SECRET=

# Server port (web dashboard is served from the same port after `npm run build`)
PORT=8787
